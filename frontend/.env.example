# Production: proxy browser API requests through the frontend origin so secure
# auth cookies and connection reuse work consistently.
BACKEND_URL=https://your-backend.example.com
NEXT_PUBLIC_API_PROXY_MODE=on
# Only set this for an intentional direct-browser API deployment.
NEXT_PUBLIC_API_URL=

# Safe rollout switch. Keep false to preserve the existing Vozon experience;
# changing this to true requires a frontend rebuild/deployment and a backend
# deployment with WHITE_LABEL_ENABLED=true.
NEXT_PUBLIC_WHITE_LABEL_ENABLED=false

# Public website-demo agent. The widget key is intentionally browser-visible;
# restrict it to approved origins in the agent's Widget settings.
NEXT_PUBLIC_WEBSITE_AGENT_ID=
NEXT_PUBLIC_WEBSITE_AGENT_PUBLIC_KEY=
# Keep this identical to backend PLATFORM_HOSTS. These hostnames always use the
# normal direct platform and are never resolved as customer white-label brands.
PLATFORM_HOSTS=vozon.ai,www.vozon.ai
